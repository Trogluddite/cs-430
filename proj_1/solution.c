/* This one is for you to finish!  Have fun! */
#include "tester.h"
#include <stdio.h>

int find_best(void*, int, size_t){
  return 0;
}

int* sorted_copy(int*, size_t){
  int someVal = 0;
  return &someVal;
}

int combine(int, int){
  return 0;
}
int main() {
  struct bundle{
    
  }
  int result = find_best(&main, 3, sizeof(size_t));
  printf("result: %d", result);

	return 0;
}
